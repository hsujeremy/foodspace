import React from 'react';

export const Error = () => {
  return (
    <div>
      <div>
        There was an error calling the Yelp Fusion API . Make sure that your
        access token is still valid!
      </div>
    </div>
  );
};

export default Error;
